# chat_complete() call sites exempt from the module-level redact() requirement
# (redact-before-inference.md). One path per line, each with a written
# justification comment. An entry here means: the input is provably redacted
# upstream. Every addition needs MV's sign-off in the PR.
#
# artifacts/landy-api/tests/test_unknown_state.py — test double: a fake LLM
# client whose chat_complete() raises immediately to force the failure path.
# No document text and no network call ever occurs; the fixture text is
# synthetic ("Pasal 1. Uji."). Added 2026-08-02 (LC-38); MV sign-off via PR.
artifacts/landy-api/tests/test_unknown_state.py
